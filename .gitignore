# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcuserstate
*.xcscmblueprint

# OS cruft
.DS_Store

# Local AI-assistant config/notes -- not for the public repo
CLAUDE.md
.claude/

# ml/ is unfinished work in progress, not ready to be tracked at all.
# It still has its own nested .gitignore for data/checkpoints/venv/rig
# build artifacts, kept in case this top-level ignore is ever narrowed
# back down once the ml/ work is actually ready to commit.
ml/
